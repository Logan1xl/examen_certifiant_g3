spring:
  datasource:
    url: jdbc:h2:mem:digitrans_crm_test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false
jwt:
  secret: digitrans-cm-crm-secret-key-2026-must-be-256bits-long
  expiration: 86400000
